package com.example.test.java8;

import java.util.Arrays;
import java.util.List;

public class FindEmployeesBySalaray {
    public static void main(String[] args) {
        /*List<Employee> employeeList = Arrays.asList(new Employee("Ramesh", 95000),
                new Employee("Rajesh", 75000),
                new Employee("Rakesh", 45000),
                new Employee("Ravi", 33000));

        List<String> highEarners = employeeList.stream().filter(emp -> emp.getSalary() > 50000).sorted((e1, e2) -> Double.compare(e2.getSalary(), e1.getSalary()))
                .map(Employee::getName).toList();
        System.out.println(highEarners);
        System.out.println(employeeList.stream().filter(emp->emp.getSalary()>50000).sorted((e1, e2) -> Double.compare(e2.getSalary(), e1.getSalary()))
                .map(Employee::getName).toList());*/
    }


}
